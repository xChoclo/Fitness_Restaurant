<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Restaurant | Taste And Train</title>

    <!-- Fuente nunito -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital@0;1&display=swap" rel="stylesheet">

    <!-- API para iconos -->
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,100..700,0..1,0..200" rel="stylesheet">

    <!-- SwiperJS CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.css"/>

    <!-- Estilos locales -->
    <link rel="stylesheet" href="/CSS/base.css">

    <% if (typeof extra_css !== 'undefined') { %>
        <%= extra_css %>
    <% } %>
</head>

<body>
    <header>
        <div class="menu">
            <a href="/"><img src="/img/logo negro.png" class="logo-1" alt="logo"></a>
            <input type="checkbox" id="menu"/>
            <label for="menu">
                <span class="material-symbols-outlined menu-icono">menu</span>
            </label>

            <nav class="navbar">
                <div class="menu-1">
                    <ul>
                        <li><a href="/">Inicio</a></li>
                        <li><a href="/menu">Menú</a></li>
                        <li>
                            <label class="switch" id="modo-oscuro">
                                <input type="checkbox" id="toggle-dark-mode">
                                <span class="slider-modo-oscuro"></span>
                            </label>
                        </li>
                    </ul>
                </div>

                <img src="/img/logo negro.png" class="logo-2 invertir-en-oscuro">

                <div class="menu-2">
                    <li>
                        <a id="premium-icon" class="btn-premium" href="/clientePremium">
                            <span class="material-symbols-outlined">workspace_premium</span>
                        </a>
                    </li>
                    <div class="separator"></div>

                    <% if (sesion_iniciada && rol_usuario === 'cliente') { %>
                        <li>
                            <a href="/perfil_cliente">
                                <span class="material-symbols-outlined">person</span>
                            </a>
                        </li>
                    <% } else { %>
                        <li>
                            <a href="/login">
                                <span class="material-symbols-outlined">login</span>
                            </a>
                        </li>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <div id="toast-container">
            <% if (messages && messages.length > 0) { %>
                <% messages.forEach(function(message) { %>
                    <div class="toast toast-<%= message.tags %>">
                        <div class="toast-icon-box toast-<%= message.tags %>">
                            <span class="material-symbols-outlined icon">
                                <% if (message.tags === 'success') { %>check
                                <% } else if (message.tags === 'error') { %>close
                                <% } else if (message.tags === 'info') { %>info
                                <% } else if (message.tags === 'warning') { %>warning
                                <% } %>
                            </span>
                        </div>
                        <div class="toast-message">
                            <strong>
                                <% if (message.tags === 'success') { %>¡Éxito!
                                <% } else if (message.tags === 'error') { %>Error
                                <% } else if (message.tags === 'info') { %>Información
                                <% } else if (message.tags === 'warning') { %>Advertencia
                                <% } %>
                            </strong>
                            <span><%= message.text %></span>
                        </div>
                        <span class="material-symbols-outlined close">close</span>
                    </div>
                <% }); %>
            <% } %>
        </div>

        <!-- Aquí irá el contenido de la página -->
        <%- body %>
    </main>

    <div class="contenedor-ia">
        <div class="chatbot-fab-container" id="chatbot-fab-container">
            <div class="loader-fixed chatbot-fab-icon" id="chatbot-icon">
                <div class="loader">
                    <div class="circle"></div>
                </div>
            </div>

            <div class="card chatbot-fab-chat" id="chatbot-container">
                <div class="bg">
                    <span class="material-symbols-outlined" id="close-btn">close</span>
                    <div class="chatbot-scroll-area">
                        <div id="chatbot-body">
                            <div id="chatbot-messages"></div>
                        </div>
                    </div>
                    <div class="chatbot-input-container" id="chatbot-input-container">
                        <input type="text" id="chatbot-input" placeholder="Hablemos un rato..." />
                        <span class="material-symbols-outlined" id="send-btn">send</span>
                    </div>
                </div>
                <div class="blob"></div>
            </div>
        </div>
    </div>  

    <footer class="footer container">
        <div class="footer-section logo-social invertir-en-oscuro">
            <img src="/img/logo negro.png" class="footer-logo">
            <div class="social-icons">
                <a href="#"><img src="/img/Facebook.png"></a>
                <a href="#"><img src="/img/Instagram.png"></a>
                <a href="#"><img src="/img/pinterest.png"></a>
            </div>
        </div>

        <div class="footer-section links">
            <ul>
                <li><a href="/terminos">Términos y Condiciones</a></li>
                <li><a href="/sobre_nosotros">Sobre nosotros</a></li>
                <li><a href="/sistema_envios">Sistema de envíos</a></li>
            </ul>
        </div>

        <div class="footer-section collaboration">
            <img src="/img/logo negro.png" class="logo-collaboration invertir-en-oscuro">
            <span class="material-symbols-outlined collaboration-x">close</span>
            <img src="/img/rappi.png" alt="Rappi-Logo" class="rappi-logo invertir-en-oscuro">
        </div>
    </footer>

    <!-- JS scripts -->
    <script src="https://cdn.jsdelivr.net/npm/swiper/swiper-bundle.min.js"></script>
    <script src="/JS/index.js"></script>
    <script src="/JS/chat-bot.js"></script>
    <script src="/JS/notificacion.js"></script>
</body>
</html>